# Media fixtures

CI uses fake `PerceptionPacket` values so no production attachment, model key, or customer data is required. Optional real-model tests may use synthetic image, audio, and short video files only.
