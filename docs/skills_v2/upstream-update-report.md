# hifleet-skills upstream update report

No candidate has been prepared yet. Run `prepare --revision <commit>` via
`scripts/skills_v2/sync_hifleet_skills.py` to generate a fresh report. A candidate
never becomes current until `apply` runs on a reviewed revision.
