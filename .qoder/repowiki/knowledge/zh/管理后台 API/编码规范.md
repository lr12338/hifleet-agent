- 所有路由处理函数均通过 Pydantic 模型（如 `LogListQuery`, `AdminTestRunRequest`）接收并验证输入参数，确保类型安全。
- 服务层函数（如 `list_logs`, `get_session_timeline`）直接返回字典或 Pydantic 模型实例，由 FastAPI 自动序列化为 JSON 响应。
- 涉及外部 API 调用的异步操作（如 `proxy_test_run`, `stream_ark_chat`）均使用 `httpx.AsyncClient` 并配合 `async/await` 语法实现非阻塞 I/O。