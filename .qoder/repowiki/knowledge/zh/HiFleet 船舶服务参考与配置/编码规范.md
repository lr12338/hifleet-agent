- 统一采用 Query 参数传递鉴权 Token（`usertoken` 或 `api_key`），POST 请求仅在 Body 中承载业务 JSON。
- API 响应遵循标准化结构，成功状态标识为 `result: "ok"` 或 `status: "ok"`，错误时返回 `code` 与 `message`。
- 执行船舶相关查询前，强制要求先通过 `shipSearchText` 或档案接口获取标准的 MMSI/IMO 标识符。