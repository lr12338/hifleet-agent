- 采用 TypedDict 定义 Agent 状态 Schema（如 `CustomerSupportState`），并通过 `Annotated` 配合 reducer 函数实现消息历史的自动截断与合并。
- 执行链路中普遍使用 Trace 对象（`HarnessTrace`）记录工具调用序列、耗时及置信度，并在最终响应前通过 `sanitize_customer_output` 进行安全清洗。
- 意图分类采用“规则优先 + LLM 兜底”策略，先通过正则和关键词匹配高频场景（如船舶查询、故障排查），未命中时再调用 LLM 进行复杂推理。