- 入口层：`__init__.py` 导出 `SKILL_TOOLS` 列表，统一暴露技能可用工具。
- 工具层：`tools.py` 定义基于 `langchain.tools.tool` 装饰器的 `inspect_media_attachment` 函数，作为核心执行单元。
- 依赖方向：内部依赖 `utils.file.file.infer_file_category` 进行文件类型推断，使用标准库 `urllib.parse` 和 `json` 处理 URL 解析与结果序列化。
- 安全边界：仅返回脱敏后的元数据（如分类、后缀、URL 有效性），严禁暴露内部路径或原始载荷。