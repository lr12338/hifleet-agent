- 所有工具函数在执行前必须调用 `_is_employee_profile()` 进行身份鉴权，非员工身份直接抛出 `PermissionError`。
- 工具执行结果统一封装为 `ToolResult` 对象，并通过 `_emit` 辅助函数上报指标，确保状态码、延迟和追踪上下文的一致性。
- 涉及文件路径的操作均通过 `_resolve_allowed_path` 进行解析与白名单校验，防止路径遍历攻击。
- 沙盒 Python 代码执行前必须经过 `_ast_guard` 静态检查，仅允许白名单内的标准库与第三方库导入。