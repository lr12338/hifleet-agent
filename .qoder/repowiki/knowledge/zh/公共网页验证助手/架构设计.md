- 模块采用扁平结构，核心逻辑集中在 `tools.py`，通过 `__init__.py` 导出 `SKILL_TOOLS` 列表。
- 基于 LangChain 的 `@tool` 装饰器定义 `verify_public_page` 工具，实现标准化接口。
- 内部集成安全校验机制，自动拦截 localhost、127.0.0.1 及 .local 域名，防止访问内部资源。
- 使用 `requests` 库执行 HTTP GET 请求，并通过正则表达式从 HTML 中提取 `<title>` 及纯文本片段。